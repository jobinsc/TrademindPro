'use client';

import NexusStrategyNoteWorkspace from '@/components/nexus-pulse/NexusStrategyNoteWorkspace';

export default function NexusStrategyNotePage() {
  return <NexusStrategyNoteWorkspace />;
}
