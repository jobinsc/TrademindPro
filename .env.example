# Optional cloud AI for Agents Hub
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Upstox (for real login + later live orders)
# Redirect URI must match exactly what you register in Upstox developer console:
#   http://localhost:3000/api/broker/upstox/callback
UPSTOX_API_KEY=
UPSTOX_API_SECRET=
UPSTOX_REDIRECT_URI=http://localhost:3000/api/broker/upstox/callback
